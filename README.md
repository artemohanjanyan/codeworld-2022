# Haskell via Sokoban

Поиграть в игру, которую вы напишете, если сделаете все ДЗ, можно [тут](https://code.world/run.html?mode=haskell&dhash=Do_TSjo_kT2I_94n5lv6w4Q).

Среда разработки: https://code.world/haskell

Шаблон и инструкция для желающих работать с кодом локально: https://github.com/artemohanjanyan/codeworld-template

Занятия ведутся по [этому материалу](https://haskell-via-sokoban.nomeata.de/).

# Лекция №1

[Запись лекции](https://youtu.be/_6pB7kkFO3Y).

Код с лекции: [GitHub](Lectures/Lec1.hs), [CodeWorld](https://code.world/haskell#PLdEYQSrD0LMQywOPfzcYcA).

## Домашнее задание №1

При решении заданий пытайтесь не просто писать хоть какой-то код, который работает, а делать его красивым и лаконичным. Постарайтесь писать маленькие функции, которые выполняют одну маленькую задачу, и затем используйте эти маленькие функции для более сложных функций. Избегайте повторений кода.

В заданиях вам предстоит создать много рисунков и анимаций. Будет описано, что от вас требуется, но вы всегда можете добавить что-нибудь от себя, дайте волю своему творчеству 💃

Присылайте решения мне в телеграм @artemohanjanyan в любом удобном вам виде, например просто ссылкой на code.world.

### 1.1 Светофор
На лекции мы сделали анимацию двуцветного светофора. Добавьте в него жёлтый свет и анимируйте следующие фазы светофора:

- Длинная фаза зелёного света;
- Короткая фаза жёлтого света;
- Длинная фаза красного света;
- Короткая фаза красного с жёлтым;
- Обратно к зелёному свету.

Можете добавить больше фаз, чтобы светофоры были больше похожи на украинские.

[Код светофора с лекции](https://code.world/haskell#Puzl_r5WNKnabTJkU4sjHgQ), можете начать с него.

Пример:

![Пример](media/traffic_light.gif)

[Решение](Solutions/1.1.hs), [Разбор](https://youtu.be/7meF_qrOqx4).

### 1.2 Цветущие деревья
На лекции мы нарисовали дерево, но оно выглядит суховато.

```haskell
tree :: Integer -> Picture
tree 0 = blank
tree n = polyline [(0,0),(0,1)] & translated 0 1 (
  rotated (pi/10) (tree (n-1)) & rotated (- pi/10) (tree (n-1)))
```

Сделайте, чтобы оно зацвело! Создайте анимацию дерева, которое сначала выглядит как сухое дерево с лекции, а затем начинает цвести на крайних ветках. В течение 10 секунд цветы на ветках должны увеличиваться. Через 10 секунд дерево должно полностью расцвести, и анимация должна остановиться.

Примеры того, как могут выглядеть цветки на деревьях: 🟡, 💮, 🌻, 🏵️.

В коде сделайте так, чтобы функция отрисовки дерева не зависила от времени и от того, как выглядит цветок.

Пример:

![Пример](media/tree.gif)

[Решение](Solutions/1.2.hs), [Разбор](https://youtu.be/2fVzBF8MB-M).

### 1.3 Поле для игры Сокобан
К концу курса вы реализуете игру Сокобан. Можете почитать про игру [на википедии](https://uk.wikipedia.org/wiki/Sokoban), там ещё есть классная анимация.

>Гра відбувається на дошці з квадратів, де кожен квадрат є підлогою або стіною. Деякі квадрати містять ящики, а деякі — позначені, як їхні місця зберігання.

>Гравець обмежений дошкою, і може рухатися горизонтально або вертикально на порожні квадрати (не через стіни або ящики). Також гравець може переміститися на місце ящика, штовхаючи його на вільне місце за ним. Ящик не можна рухати на  інший ящик або стіну. Кількість ящиків дорівнює кількості місць зберігання. Головоломка вирішена, коли всі ящики опинились на місцях зберігання.

В этом задании надо научиться рисовать всё, что нам потребуется для отрисовки игрового поля:
1. Стены;
2. Землю, т.е. пустое пространство;
3. Землю с меткой, на которую нужно подвинуть ящик;
4. Ящики.

Можете начать с [этого кода](https://code.world/haskell#PhaxQVa5_9uWXva_quQwPcA).

Реализуйте функции `wall`, `ground`, `storage` и `box` типа `Picture`, которые рисуют соответствующие поля. Каждое должно быть ширины и высоты 1, располагаться в центре. Чем поля красивее, тем в игру приятнее играть 🙂

Реализуйте функцию `drawTile :: Integer -> Picture`, которая рисует поле соответствующего номера от 1 до 4 из списка выше. Если аргумент функции это не число от 1 до 4, функция не должна ничего рисовать (но и не должна падать).

Уровень игры может быть представлен как фукнция типа `Integer -> Integer -> Integer`, которая по координатам клетки возвращает номер вида поля из списка выше. Пока что у нас будет один уровень:

```haskell
maze :: Integer -> Integer -> Integer
maze x y
  | abs x > 4  || abs y > 4  = 0
  | abs x == 4 || abs y == 4 = 1
  | x ==  2 && y <= 0        = 1
  | x ==  3 && y <= 0        = 3
  | x >= -2 && y == 0        = 4
  | otherwise                = 2
```

Реализуйте функцию, которая отрисовывает этот уровень:

```haskell
pictureOfMaze :: Picture
```
Она должна для каждой координаты (x, y), где x, y ∈ [-10…10], определить вид поля с помощью функции `maze`, отрисовать его с помощью функции `drawTile`, и с помощью функции `translated` переместить поле на соответствующую координате позицию.

Нужно написать что-то вроде функции `manyTrafficLights` с лекции. Скорее всего, у вас будет одна рекурсивная функция, которая будет отрисовывать строчку за строчкой, и строчки, в свою очередь, будут отрисовываться другой рекурсивной функцией, которая будет отрисовывать поле в каждом столбце соответствующей строчки.

Фукция `main` должна рисовать `pictureOfMaze`.

Пример:

![Пример](media/sokoban_map.jpg)

[Решение](Solutions/1.3.hs), [Разбор](https://youtu.be/OtoZRp8twbY).

# Лекция №2

[Запись лекции](https://youtu.be/GgmFWDZSqLE).

Код с лекции: [GitHub](Lectures/Lec2.hs), [CodeWorld](https://code.world/haskell#P_zf_4OMwaBegYiSnS16xUQ).

## Домашнее задание №2

Для решения ДЗ вы можете начать с [кода с лекции](https://code.world/haskell#P_zf_4OMwaBegYiSnS16xUQ), или со своего решения предыдущего ДЗ, если вы там нарисовали что-то получше одноцветных квадратиков.

По необходимости используйте локальные определения и лямбда-выражения.

### 2.1 Двигающийся человечек

Определите значение `player :: Picture`, которое будет отрисовывать человечка.

Определите `main` с помощью `activityOf` так, чтобы:
- человечек рисовался поверх игрового поля,
- изначально он находился на земле,
- стрелки передвигали человечка по игровому полю (поле двигаться не должно),
- человечек передвигался только на поля земли или земли с меткой. При попытке передвинуться на ящик или стену человечек должен оставаться на текущем поле. 

[Пример](https://code.world/run.html?mode=haskell&dhash=Dc_b4IxNW5al9Tdbd6lWjLw).

### 2.2 Поворачивающийся человечек
Теперь мы хотим сделать так, чтобы человечек смотрел по направлению движения. Сначала переопределите человечека функцией `player :: Direction -> Picture`, которая будет рисовать его в одном из четырёх вариантов.

После этого измените `main` таким образом, чтобы человечек смотрел в ту сторону, в которую он пытался пойти на предыдущем ходу.

Подсказка: сначала подумайте, какое у вас должно быть состояние.

[Пример](https://code.world/run.html?mode=haskell&dhash=Dr4pJqT05HUuOSmv2njd9qg).

### 2.3 Перезапуск
Было бы неплохо иметь возможность начать игру с начала. Это в принципе довольно полезная функциональность, что для нашей игры, что для любой другой интерактивной программы, так что давайте решим задачу перезапуска в общем случае.

Реализуйте следующую функцию
```haskell
resetableActivityOf ::
    world ->
    (Event -> world -> world) ->
    (world -> Picture) ->
    IO ()
```
у которой тип такой же, как и у `activityOf`.

Эта функция должна себя вести так же, как `activityOf` (которую она, конечно же, будет внутри вызывать), но по нажатию на `Esc` событие не должно передаваться в обработчик событий. Вместо этого, состояние программы должно меняться на начальное состояние.

В определении `resetableActivityOf` используйте локальные функции и/или лямбда-выражения, и не используйте другие top-level определения.

Воспользуйтесь `resetableActivityOf` вместо `activityOf` в `main`.

[Пример](https://code.world/run.html?mode=haskell&dhash=Ds_7PpypE0pjLFSY8qNcRTA).

### 2.4 Новый уровень (по желанию)
Создайте свой уровень. Он должен отвечать нескольким требованиям:
- должен использовать координаты от -10 до 10,
- должен быть связным (не должно быть недостижимых полей земли),
- должен быть замкнутым (игрок не должен иметь возможность выйти на пустые поля),
- должен быть решаемым ([вот](https://uk.wikipedia.org/wiki/Sokoban#%D0%9F%D1%80%D0%B0%D0%B2%D0%B8%D0%BB%D0%B0) правила).

### Разбор

[Решение](Solutions/2.hs), [Разбор](https://youtu.be/qnup96qFTl8).

### Дополнительное задание

[GitHub](Extra/ChurchEncoding.hs), [CodeWorld](https://code.world/haskell#P4U9RnIZOxKDPvDTR7gkF1g).

# Лекция №3

[Запись лекции](https://youtu.be/WKi5GJwNpL0).

Код с лекции: [GitHub](Lectures/Lec3.hs), [CodeWorld](https://code.world/haskell#PlEzzsVFMnXxLjwn9CU8UjQ).

## Домашнее задание №3

Цель задания – реализовать игру Сокобан!

Чтобы вам было проще, задание разбито на маленькие шаги.

Можете начать с [этого шаблона](https://code.world/haskell#PhSM6EUq1z5iFxJQM52PHTg).
Там приведены сигнатуры некоторых функций, которые вам предлагается реализовать в заданиях.
Всё, что помечено как `undefined` или `FIXME`, вам нужно реализовать или переписать.
Код для отрисовки игрока и тайлов (и `maze` из задания 2.4, если делали его) тоже замените на свой.

### 3.1 Состояние

Определите `State` так, чтобы он хранил состояние игры:
- текущую позицию игрока;
- направление, куда он смотрит;
- текущее положение всех коробок.

### 3.2 Начальное состояние

Определите значение `initialState :: State` для начального состояния:
- поставьте игрока на захардкоженную коордитану земли или земли с меткой;
- используйте произвольное направление;
- найдите координаты всех коробок.

Последний пункт нужно не хардкодить, а как-то достать из функции `maze`.
Определите значение `initialBoxes :: List Coord`.
Для этого нужно пройтись по всем координатам уровня (точки (x, y), -10 ≤ x, y ≤ 10),
и добавить в список те координаты, на которых стоят коробки.

Есть два способа это сделать, реализуйте один из них, но попытайтесь понять оба.

1. Определите функцию `appendList :: List a -> List a -> List a`, которая склеивает два списка.
Затем реализуйте `initialBoxes` по аналогии с `pictureOfMaze`, только вместо `&` используйте `appendList`.

2. Или можете передавать список как параметр в рекурсивную функцию, которая перебирает все координаты.
Изначально список пустой (`Empty`).
Если на рассматриваемой координате стоит не коробка, то мы не меняем список, и просто передаём его дальше.
Если на рассматриваемой координате коробка, то мы расширяем список конструктором `Entry`, и передаём дальше новый список.
Вспомогательная функция может иметь тип `go :: Integer -> Integer -> List Coord -> List Coord`.
Такой дополнительный параметр, в котором постепенно собирается результат нашей операции, называют _аккумулятором_.

Протестировать `initialBoxes` можно так:

```haskell
main = drawingOf (pictureOfBoxes initialBoxes)
```

### 3.3 Вспомогательные функции для работы с уровнем

Функция `maze` возвращает нам коробки в начальных позициях.
Но по мере игры коробки будут двигаться, и просто одной функцией `maze` пользоваться будет не очень удобно.

- Мы уже определили `noBoxMaze :: Coord -> Maze`, которая работает как `maze`, только вместо `Box` возвращает `Ground`.
Используйте её в `pictureOfMaze`.

- Определите `mazeWithBoxes :: List Coord -> (Coord -> Maze)`, которая работает как `noBoxMaze`, если список не содержит переданную координату,
и возвращает `Box`, если координата содержится в списке.
Для этого (и не только) будет удобно определить вспомогательную функцию
`eqCoord :: Coord -> Coord -> Bool`
для проверки координат на равенство.

Заметьте, что если к фукнции `mazeWithBoxes` частично применить первый аргумент (список текущих позиций коробок),
то тип результата будет совпадать с типом функции `maze`.

### 3.4 Отрисовка состояния

Реализуйте

```haskell
draw :: State -> Picture
```

Функция должна отрисовывать три вещи:
- игрока;
- коробки в нужных позициях (используйте `pictureOfBoxes`);
- уровень (используйте `pictureOfMaze`, который использует `noBoxMaze`).

### 3.5 Обработка событий

Реализуйте

```haskell
handleEvent :: Event -> State -> State
```

Регируйте только на нажатия на стрелки.
В результате игрок либо успешно двигается на новую клетку, либо остаётся на месте.

Игрок двигается на новую клетку, если новый тайл это земля, земля с меткой или коробка.
Если это коробка, то следующий за той коробкой тайл в том же направлении должен быть землёй или землёй с меткой.
В противном случае игрок остаётся на месте.

Если игрок успешно двигается на новую клетку,
то нужно обновить в состоянии координату игрока, направление, в котором мы подвинулись, и координаты коробок.

Подсказка (можете делать это иначе). Для обновления координат коробок определите функцию
```haskell
moveFromTo :: Coord -> Coord -> (Coord -> Coord)
```
которая принимает
- начальную координату, с которой мы возможно двигаем коробку,
- конечную координату, на которую мы возможно двигаем коробку,
- и координату коробки.

Если первый параметр совпадает с третьим, то нужно вернуть второй параметр, иначе нужно вернуть третий параметр.
Такая функция не будет менять координаты коробок, которые мы не двигаем,
так что её можно применить ко всем коробкам с помощью функции `mapList`.

Если игрок остаётся на месте, обновите в состоянии только направление.

### 3.6 Собираем всё вместе

Определите `Activity` с помощью функций, реализованных на предыдущих шагах.
Оберните её в `resetable` и `withStartScreen`.

В результате должно получиться [что-то такое](https://code.world/run.html?mode=haskell&dhash=DiKJ9rAdfymDR3Ndt3JJCcg).

### 3.7 Поздравляем игрока с победой

Чего-то не хватает.
Надо бы научитсья определять, пройден ли уровень, и останавливать игру, если он пройден.

Определите функцию

```haskell
isWon :: State -> Bool
```
которая возвращает `True`, если уровень пройден.
Уровень пройден, если каждая коробка стоит на земле с меткой.
Определите две вспомогательные функции:

1. `isOnStorage :: Coord -> Bool`, которая возвращает `True`, если координата соответствует земле с меткой, иначе `False`.
2. `allList :: List Bool -> Bool`, которая возвращает `True`, если все значения в списке это `True`, иначе `False`.
Для пустого списка она должна возвращать `True`.

С помощью этих двух функций и `mapList` можно реализовать `isWon`.

Используйте `isWon` в двух местах:

1. В `draw` если уровень пройден, отрисуйте текст "You won!" (ну или что-то такое) поверх всего уровня.
2. В `handleEvent` если уровень пройден, игнорируйте все события.

После этого должно выйти [что-то такое](https://code.world/run.html?mode=haskell&dhash=DsI0_d27eHv57bhp0lZNv0A).

Вас я тоже поздравляю! Вы написали игру без единой переменной 🙂

[Решение](Solutions/3.hs), [Разбор](https://youtu.be/gjj5syib1TA).
